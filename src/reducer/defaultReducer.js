const DEFAULT_LOAD = 'react-native/default/DEFAULT_LOAD';
const DEFAULT_SUCCESS = 'react-native/default/DEFAULT_SUCCESS';
const DEFAULT_FAIL = 'react-native/default/DEFAULT_FAIL';

const initState = {
  user: {}
}

export default defaultReducer = (state = initState, action={}) => {
  switch (action.type) {
    default: 
      return state;
  }
}